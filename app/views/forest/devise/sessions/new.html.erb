<div class="row">
  <div class="col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
    <div class="panel panel-default">
      <%# TODO: make this if User.admin? or something like that %>
      <% if User.first %>
        <%= render 'default_login' %>
      <% else %>
        <%= render 'first_time_login' %>
      <% end %>
    </div>
  </div>
</div>
