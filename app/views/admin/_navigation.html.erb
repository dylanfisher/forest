<nav class="nav navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
    <%= link_to 'Dashboard', admin_path, class: 'navbar-brand' %>
    <%= render 'admin/shared/navigation/navbar' %>
    <ul class="nav navbar-nav ml-auto">
      <% if policy(:setting).dashboard? %>
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Settings <span class="caret"></span>
          </a>
          <div class="dropdown-menu">
            <%= link_to 'General', admin_settings_path, class: "dropdown-item #{admin_navbar_active_class(admin_settings_path)}" %>
            <div class="dropdown-divider"></div>
            <%= link_to 'Clear Cache', admin_cache_purge_index_path, class: 'dropdown-item', data: { confirm: "Are you sure you wan't to clear the website cache? Performance may decrease while cache rebuilds." } %>
          </div>
        </li>
      <% end %>
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
          <%= current_user.display_name %> <span class="caret"></span>
        </a>
        <div class="dropdown-menu">
          <%= link_to 'Edit My Profile', edit_admin_user_path(current_user), class: 'dropdown-item' %>
          <%= link_to 'View Home Page', root_path, class: 'dropdown-item' %>
          <%= link_to 'Forest Docs', admin_documentation_path, title: 'Learn more about this CMS and how to use it.', class: 'dropdown-item' %>
          <div class="dropdown-divider"></div>
          <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
        </div>
      </li>
    </ul>
  </div>
</nav>
<div class="nav-spacer"></div>
